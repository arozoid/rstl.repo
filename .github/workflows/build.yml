name: Build Packages

on:
  push:
    branches: [main]
  schedule:
    - cron: '0 12 * * *'  # daily at UTC noon
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: archlinux:base-devel
      options: --user root
    steps:
      - name: Install deps
        run: pacman -Syu --noconfirm pacman-contrib git rust meson scdoc tllist xorgproto fcft libpulse systemd-libs libxcb libyaml pipewire pixman wayland wayland-protocols xcb-util xcb-util-cursor xcb-util-wm alsa-lib libinih polkit xdg-desktop-portal base-develgtk-update-icon-cache

      - name: Add build user
        run: |
          useradd -m builder
          echo "builder ALL=(ALL) NOPASSWD: ALL" >> /etc/sudoers

      - uses: actions/checkout@v4

      - name: Build packages
        run: |
          mkdir -p repo
          for dir in pkgbuilds/*/; do
            pkg=$(basename "$dir")
            echo "::group::Building $pkg"
            builddir="/tmp/build-$pkg"
            mkdir -p "$builddir"
            cp -a "$dir"/. "$builddir/"
            chown -R builder:builder "$builddir"
            sudo -u builder bash -c "
              cd '$builddir'
              makepkg --noconfirm --nocheck --skipchecksums --skipinteg --skippgpcheck 2>&1
            "
            mv "$builddir"/*.pkg.tar.* repo/ 2>/dev/null || true
            rm -rf "$builddir"
            echo "::endgroup::"
          done

      - name: Create repo database
        run: |
          chown -R builder:builder repo
          sudo -u builder bash -c "
            cd repo
            repo-add rstl-repo.db.tar.gz *.pkg.tar.*
          "

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: repo

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
